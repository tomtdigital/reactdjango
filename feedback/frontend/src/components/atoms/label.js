import styled from 'styled-components';

const Label = styled.label`
  display: block;
  margin-bottom: 0.5em;
  margin-top: 0.5em;
  input,
  textarea {
    display: block;
  }
`;

export default Label;
